# AGENTS.md

## Repository Purpose

This is the GitHub profile README repository for `tarcisiopgs`.

The main file is `README.md`. There is no application code, dependency
manifest, package manager, build step, or automated test suite here.

## Editing Rules

- Keep changes focused on the public profile README.
- Preserve the concise personal profile tone unless the user asks for a rewrite.
- Do not add generated artifacts, package manager files, or tooling just to edit
  Markdown.
- Keep badges, external links, and contact links valid when modifying them.
- Prefer simple Markdown. Avoid custom HTML unless it is already present or
  needed for GitHub profile rendering.

## Verification

- Run `git diff --check` after edits.
- If links or badges are changed, manually inspect the rendered Markdown or
  verify the target URLs when practical.
